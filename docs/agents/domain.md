# Domain Docs

This repository uses the single-context layout.

Before exploring a domain, read `CONTEXT.md` at the repository root when it
exists, followed by relevant decisions under `docs/adr/`. If either is absent,
continue without treating that absence as a problem. Create glossary and ADR
files lazily when domain modeling resolves a term or durable decision.
